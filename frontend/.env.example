# =====================================================
# FRONTEND ENVIRONMENT VARIABLES
# =====================================================
# For Next.js deployment only
# Copy this file to .env.local for local development
# or configure in your hosting platform (Vercel, Netlify, etc.)

# =====================================================
# API Configuration (Required)
# =====================================================
# Backend API endpoint URL
# Development: http://localhost:8000
# Production: https://api.yourdomain.com (your deployed backend)
NEXT_PUBLIC_API_URL=http://localhost:8000

# =====================================================
# IMPORTANT SECURITY NOTES
# =====================================================
# - Only NEXT_PUBLIC_* variables are exposed to the browser
# - NEVER put API keys or secrets in NEXT_PUBLIC_* variables
# - All secrets must remain on the backend
